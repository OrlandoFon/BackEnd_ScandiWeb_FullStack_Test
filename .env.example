# Database Configuration
# MySQL container hostname
DB_HOST=mysql_db

# Database name for the application
DB_NAME=ecommercedata

# MySQL user credentials
DB_USER=scandiweb_user
DB_PASSWORD=scandiweb_password

# MySQL root password (for initial setup)
DB_ROOT_PASSWORD=root

# Testing Configuration
# Set to 1 to enable tests, 0 to disable
TESTING=0
